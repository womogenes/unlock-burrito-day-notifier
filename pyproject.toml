[project]
name = "unlock-burrito-day-notifier"
version = "0.1.0"
description = "Check unlockburritoday.com and email on redirect failures"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
]
